import { identifySharedCharacteristicsShared } from './identify-shared-characteristics-shared'

describe('identifySharedCharacteristicsShared', () => {
  it('should work', () => {
    expect(identifySharedCharacteristicsShared()).toEqual(
      'identify-shared-characteristics-shared',
    )
  })
})
